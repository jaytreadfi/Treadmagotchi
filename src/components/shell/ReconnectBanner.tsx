'use client';

export default function ReconnectBanner() {
  return (
    <div className="fixed top-0 left-0 right-0 z-50 bg-hp text-white text-center py-2 text-[11px] font-pixel animate-scan-pulse">
      RECONNECTING...
    </div>
  );
}
